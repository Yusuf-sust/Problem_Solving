#include <stdio.h>
#include <math.h>

int main()
{
    int x, y, z, i, n, a;
    scanf("%d", &n);
    for(i = 0; i < n; ++i){
        scanf("%d%d%d", &x, &y, &z);
        a = 1;
        power = x % z;

    }
}
