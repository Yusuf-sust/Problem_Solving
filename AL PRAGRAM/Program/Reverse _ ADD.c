#include <stdio.h>

int rev(int i);
int ra(int j);
int main()
{
    int n, m, t;
    scanf("%d", &n);
    while(!m){
        int r = rev(n);
        if(r == 1){
            break;
        }
    }
}
