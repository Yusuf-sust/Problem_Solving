#include <stdio.h>
#include <math.h>

int main()
{
    int a;
    int b;
    b = pow(2,31) - 1;
    long long int sum;
    sum = pow(2,31) - 1;
    printf("Sum is %d %lld", b, sum);

    return 0;

}
