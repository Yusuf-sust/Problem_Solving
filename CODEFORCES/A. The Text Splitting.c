#include <stdio.h>
#include <string.h>

int main()
{
    int i, j, k = 0, p, q;
    char s[1000]
    scanf("%d %d %d", &k, &p, &q);
    scanf("%s", s);
    if(p > q){
        int temp = p; p = q; q = temp;
    }
    if(k - p < p)
        printf("-1\n");
    else{
        for(i = p; i < q; i++){

        }
    }
}
